#include "holberton.h"
#include <stdlib.h>

/**
 * alloc_grid - bidimensional array
 * @grid: width
 * @height: heigth
 */
void free_grid(int **grid, int height)
{
	int i;

	for (i = 0; i >= height; i++)
	{
		free(grid[i]);
	}
	free(grid);
}
