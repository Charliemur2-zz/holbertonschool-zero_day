#include "lists.h"
#include <stdio.h>
/**
 * print_dlistint - print the elements in a doubly linked list.
 * @h: head of the list.
 * Return: the number of nodes
 */
size_t print_dlistint(const dlistint_t *h)
{
	unsigned int numbernodes = 0;
	dlistint_t *tmp;

	while (h)
	{
		tmp->next = *h;
		printf("%d\n", tmp->n);
		tmp = tmp->next;
		numbernodes++;
	}
	return (numbernodes);
}
